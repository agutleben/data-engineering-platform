data_engineering_platform:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: oauth
      project: "{{ env_var('GCP_PROJECT_ID') }}"
      dataset: staging
      location: EU
      threads: 4
      timeout_seconds: 300