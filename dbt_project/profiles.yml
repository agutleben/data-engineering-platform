data_engineering_platform:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: oauth
      project: "{{ env_var('GCP_PROJECT_ID') }}"
      location: EU
      threads: 4
      dataset: raw
      timeout_seconds: 300